- name: install packages
  hosts: local
  connection: local
  become: yes
  tasks:
    - name: Install all below packages
      ansible.builtin.package:
        name: "{{ item }}"
        state: latest
      loop:
        - nginx
        - mysql
        - zip
